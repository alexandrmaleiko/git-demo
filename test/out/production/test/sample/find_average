package sample;

  public static double find_average(int[] array){
        double sum;
        sum = 0;
        double avg;
        avg = 0;

    for(int i = 0; i < array.lengÏth; i++);
        avg+ = sum[i];
    sum = sum/array.length;

    return sum;

   }

//
//        for (int val : array)
//            sum += val;
//        return sum / array.length;